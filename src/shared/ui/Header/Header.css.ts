import { style } from '@vanilla-extract/css';

export const headerWrap = style({
  display: 'flex',
  alignItems: 'center',
});

export const logoStyle = style({
  fontSize: '3.2rem',
  lineHeight: '1.2',
});
